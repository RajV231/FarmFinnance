// Not using React Router to keep it lightweight as requested ("No overengineering"). 
// State-based routing handles the flow perfectly for a linear simulation.
export const ROUTES = {
  HOME: '/',
};